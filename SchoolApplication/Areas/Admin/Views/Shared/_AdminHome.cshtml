@*http://www.w3schools.com/bootstrap/bootstrap_case_navigation.asp*@

@if (Request.IsAuthenticated)
{
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="../AdminProfile/Home"><span class="glyphicon glyphicon-user"></span>@Html.Encode(" Welcome " + User.Identity.Name)</a>
            </div>
            <div class="collapse navbar-collapse navbar-right" id="myNavbar">
                <ul class="nav navbar-nav">
                    <li class="dropdown">
                        <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                            @*@Html.ActionLink("Deparment", "Listing", "Department")*@ Deparment
                                <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("Add New", "Add", "Department")</li>
                            <li>@Html.ActionLink("View Deparments", "Listing", "Department")</li>
                        </ul>
                    </li>
                    <li class="dropdown">
                        <a class="dropdown-toggle" data-toggle="dropdown" href="#">Subject
                                <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("Add New", "Add", "Subject")</li>
                            <li>@Html.ActionLink("View Subjects", "Listing", "Subject")</li>
                        </ul>
                    </li>
                    <li class="dropdown">
                        <a class="dropdown-toggle" data-toggle="dropdown" href="#">Teacher
                                <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("Add New", "Add", "Teacher")</li>
                            <li>@Html.ActionLink("View Teachers", "Listing", "Teacher")</li>
                        </ul>
                    </li>
                    <li class="dropdown">
                        <a class="dropdown-toggle" data-toggle="dropdown" href="#">Time Table
                                <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("Add New", "Add", "TimeTable")</li>
                            <li>@Html.ActionLink("View Time Table", "Listing", "TimeTable")</li>
                        </ul>
                    </li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    @*<li><span class="glyphicon glyphicon-user"></span>@Html.Encode("Welcome " + User.Identity.Name)</li>*@
                    <li>@Html.RouteLink("Logout", new { action = "LogOut", controller = "User", area = "" }, new { @class = "btn btn-default" })</li>
                </ul>
            </div>
        </div>
    </nav>
}
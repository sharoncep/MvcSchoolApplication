@if (Request.IsAuthenticated)
{
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="../TeacherProfile/Home"><span class="glyphicon glyphicon-user"></span>@Html.Encode(" Welcome " + User.Identity.Name)</a>
            </div>
            <div class="collapse navbar-collapse navbar-right" id="myNavbar">
                <ul class="nav navbar-nav">
                    <li class="dropdown">
                        <a class="dropdown-toggle" data-toggle="dropdown" href="#">Student
                            <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("Add New", "Add", "Student")</li>
                            <li>@Html.ActionLink("View Students", "Listing", "Student")</li>
                        </ul>
                    </li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    @*<li><span class="glyphicon glyphicon-user"></span>@Html.Encode("Welcome " + User.Identity.Name)</li>*@
                    <li>@Html.RouteLink("Logout", new { action = "LogOut", controller = "User", area = "" }, new { @class = "btn btn-info" })</li>
                </ul>
            </div>
        </div>
    </nav>
}
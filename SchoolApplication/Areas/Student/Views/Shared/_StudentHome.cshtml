@if (Request.IsAuthenticated)
{
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="../StudentProfile/Home"><span class="glyphicon glyphicon-user"></span>@Html.Encode(" Welcome " + User.Identity.Name)</a>
            </div>
            <div class="collapse navbar-collapse navbar-right" id="myNavbar">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Personal Profile", "PersonalProfile", "StudentProfile")</li>
                    <li>@Html.ActionLink("Academic Profile", "AcademicProfile", "StudentProfile")</li>
                    <li>@Html.RouteLink("Change Password", new { action = "ChangePassword", controller = "StudentProfile" })</li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li>@Html.RouteLink("Logout", new { action = "LogOut", controller = "User", area = "" }, new { @class = "btn btn-info" })</li>
                </ul>
            </div>
        </div>
    </nav>
    
}